apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-dep
  labels:
    app: todo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      containers:
        - name: todo-app
          image: turtvaiz/todo-app:1.12-2
          volumeMounts:
            - mountPath: /app/cache
              name: img-vol
      volumes:
       - name: img-vol
         emptyDir: {}